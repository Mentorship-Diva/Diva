import 'package:flutter/material.dart';

class AppMainButton extends StatelessWidget {
  const AppMainButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
